new Crawler({
  appId: "OQQO4TVA7K",
  indexPrefix: "",
  rateLimit: 8,
  maxUrls: 100,
  schedule: "on the 8 day of the month",
  startUrls: ["https://www.oneao.cn"],
  sitemaps: [],
  saveBackup: false,
  ignoreQueryParams: ["source", "utm_*"],
  actions: [
    {
      indexName: "docusaurus_crawler_pages",
      pathsToMatch: ["https://www.oneao.cn/**", "https://oneao.cn/**"],
      recordExtractor: ({ url, $, helpers, contentLength, fileType }) => {
        return helpers.page({ $, url, contentLength, fileType });
      },
    },
  ],
  initialIndexSettings: {
    docusaurus_crawler_pages: {
      distinct: true,
      attributeForDistinct: "url",
      searchableAttributes: [
        "unordered(keywords)",
        "unordered(title)",
        "unordered(description)",
        "url",
      ],
      customRanking: ["asc(depth)"],
      attributesForFaceting: ["category"],
    },
  },
  apiKey: "a136176e9080c550121b558bc78784ec",
});